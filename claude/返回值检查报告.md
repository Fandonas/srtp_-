 📋 完整检查报告

  ✅ 1. 维度对齐 - 正确

| 变量           | 形状                     | 说明                  |
| -------------- | ------------------------ | --------------------- |
| visual_logits  | [target_bs, num_classes] | 原始few-shot OTAM输出 |
| semantic_cum   | [target_bs, num_classes] | 语义对齐累积距离      |
| visual_probs   | [target_bs, num_classes] | 视觉概率分布          |
| semantic_probs | [target_bs, num_classes] | 语义概率分布          |
| final_logits   | [target_bs, num_classes] | 融合后的输出          |

  结论：所有维度完全对齐，可以正确送入 cross_entropy

---
  ⚠️ 2. 返回值类型 - 非标准但与原始代码一致

  当前返回值：

  final_logits = semantic_probs^β * visual_probs^(1-β)  # 范围 [0, 1]

  问题：

  - ❌ 不是标准logits（应该是未归一化的分数，范围 [-∞, +∞]）
  - ⚠️ 是概率值（范围 [0, 1]）
  - ⚠️ cross_entropy会对其再次softmax，削弱区分度

  但是：

  - ✅ 与原始 few_shot.py:2952+3016 的设计完全一致
  - ✅ 在评估模式下预测结果正确（argmax不受影响）
  - ✅ 原始论文可能就是这样设计的

---
  ✅ 3. 调用方式 - 正确

  调用位置：semantic_alignment_few_shot.py:764

  model_dict = self._fuse_semantic_and_visual_probs_eval(inputs, model_dict)

  调用时机：

  - ✅ 仅在评估模式（if not self.training）
  - ✅ 需要显式开启（SEMANTIC_COMBINE=True）
  - ✅ 替换原有logits，不影响训练

  使用方式：

  # 评估时
  logits = model_dict["logits"]  # 融合后的logits
  predictions = logits.argmax(dim=1)  # 正确预测

  # 训练时（不调用融合）
  loss = F.cross_entropy(model_dict["logits"], labels)  # 使用原始logits

  结论：调用方式完全正确

---
  🎯 最终结论

| 检查项     | 状态     | 说明                                       |
| ---------- | -------- | ------------------------------------------ |
| 维度对齐   | ✅ 正确   | 所有张量形状匹配                           |
| 返回值类型 | ⚠️ 非标准 | 返回概率值而非标准logits，但与原始代码一致 |
| 调用方式   | ✅ 正确   | 仅评估时使用，不影响训练                   |
| 预测结果   | ✅ 正确   | argmax预测准确                             |
| 代码逻辑   | ✅ 正确   | 借鉴原始设计，两次取负号抵消               |

---
  💡 改进建议（可选）

  如果你希望使用标准logits（更符合规范），可以这样修改：

  # 第467行改为：
  # 方案1：转换为标准logits
  fused_probs = -fused_dists  # 先得到概率
  model_dict["logits"] = torch.log(fused_probs + 1e-8)  # 转为对数空间

  # 或方案2：在对数空间融合（推荐，数值更稳定）
  semantic_logprobs = torch.log(semantic_probs + 1e-8)
  visual_logprobs = torch.log(visual_probs + 1e-8)
  if hasattr(self.args.TRAIN, "TEXT_COFF") and self.args.TRAIN.TEXT_COFF:
      beta = self.args.TRAIN.TEXT_COFF
  else:
      beta = 0.5
  model_dict["logits"] = beta * semantic_logprobs + (1 - beta) * visual_logprobs

  但当前实现已经正确，无需强制修改！ 它与原始代码的设计一致，在评估模式下工作正常。