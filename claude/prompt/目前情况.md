---
  文本标签编码前是否加 Prompt？                                                                                                                        
                                                                                                                                                       
  结论：取决于配置，当前实验日志确认：不加任何 prompt                                                                                                  
                                                                                                                                                       
---
  分支逻辑（few_shot.py 第 2735-2748 行）

  if hasattr(self.args.TEST, "PROMPT") and self.args.TEST.PROMPT:
      # 有 PROMPT 配置 → 用模板格式化
      text_templete = [self.args.TEST.PROMPT.format(class_name) for ...]
  else:
      # 无 PROMPT 配置 → 直接用裸类别名
      text_templete = ["{}".format(class_name) for ...]

  两种路径对比：

![image-20260325173514835](C:\Users\ASUS\AppData\Roaming\Typora\typora-user-images\image-20260325173514835.png)

---
  当前 5shot 实验的实际情况

  训练日志第 345 行明确打印：

  使用默认 PROMPT: '{}'（不加前缀）

  即 TEST.PROMPT 未配置，所有类别标签直接裸编码，不加任何前缀。

---
  语义对齐分支（_create_stage_text_features，第 169 行）情况不同

  stage_text = f"a video of {stage}"
  text_tokens = tokenize([stage_text]).to(device)
  stage_feature = self._full_clip_model.encode_text(text_tokens)

  语义阶段文本硬编码加了 "a video of " 前缀，与主干路径的 PROMPT 配置完全无关。

---
  三处文本编码的 Prompt 汇总

![image-20260325173557377](C:\Users\ASUS\AppData\Roaming\Typora\typora-user-images\image-20260325173557377.png)