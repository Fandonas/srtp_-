  修改总结

  删除的内容（文本对齐）：

  1. 训练模式和评估模式的 forward 方法：

    - 删除了 text_features_train 和 text_features_test 的使用
    - 删除了 context_support 和 mid_layer 的调用
    - 删除了文本特征与视觉特征的拼接操作 torch.cat([support_features, context_support], dim=1)
    - 删除了 [:,:NUM_INPUT_FRAMES,:] 切片操作（因为不再有额外的文本token）
  2. loss 方法：

    - 删除了 classification_loss（视觉-文本分类损失）
    - 将损失项名称从 text_similarity_loss 改为 frame_alignment_loss，更准确地反映其含义
  3. 类文档字符串：

    - 更新了描述，明确说明只保留帧对齐和语义对齐，不使用类别级别的文本对齐

  保留的内容（帧对齐和语义对齐）：

  1. 帧对齐：

    - 使用 get_feats 获取视觉特征
    - 使用 context2（Temporal Transformer）对特征进行增强
    - 调用父类的 forward 获取帧对齐结果
  2. 语义对齐：

    - _parse_semantic_stages：解析语义阶段
    - _create_stage_text_features：为语义阶段创建文本特征
    - _compute_semantic_alignment_loss：计算语义对齐损失
    - _compute_semantic_alignment_loss_multi_class：多类别语义对齐
    - _fuse_semantic_and_visual_probs_eval：评估模式下融合语义和视觉概率

  现在模型只使用视觉特征进行Temporal Transformer增强，不再拼接类别级别的文本特征，完全保留了帧对齐和语义对齐的功能。