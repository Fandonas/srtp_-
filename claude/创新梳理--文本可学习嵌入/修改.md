---
  修改内容总结                                                                                                                                                                             
                                                                                                                                                                                           
  改动1：__init__ 新增 query_embed_train（第53-60行）                                                                                                                                      
                  
  num_train_classes = self.text_features_train.shape[0]
  self.query_embed_train = nn.Embedding(num_train_classes, self.mid_dim)
  self.query_embed_train.weight = nn.Parameter(
      self.text_features_train.detach().clone()
  )

  - .detach() —— 断开与 CLIP 编码器计算图的联系，不影响父类冻结逻辑
  - .clone() —— 独立的内存副本，text_features_train（父类的冻结查找表）保持不变
  - 新增显存 = num_train_classes × mid_dim × 4 bytes，例如 64类 × 512维 ≈ 128 KB，可忽略不计

  改动2：训练分支 forward 第538行

  # 改前（冻结）：
  context_support = self.text_features_train[support_real_class.long()].unsqueeze(1)

  # 改后（可学习）：
  context_support = self.query_embed_train.weight[support_real_class.long()].unsqueeze(1)

  输出形状完全相同：[support_bs, 1, mid_dim]，后续 mid_layer → cat → context2 → 截断的所有代码一行未动。

  未改动部分

  - 测试分支第680行仍使用 self.text_features_test（novel 类无法预训练，保持冻结 CLIP）
  - use_eval_text / use_combine 路径调用 super().forward()，由父类处理，不受影响
  - 语义对齐损失路径完全不涉及 context_support，无需改动